define([
  '#qt_core/controllers/all',
  './controllers/audio'
],

function (A,Controller) {
  'use strict';

  return function (options) {
    this.controller = new Controller({
      
    });
    
   
  };

});
